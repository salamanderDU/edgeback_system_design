CRITICAL: Before continuing, you MUST read PROJECT_MANIFEST.yaml and AGENTS.md first. Then read every file in the manifest read_order before writing any code. Treat the numbered design documents as binding requirements.

Resume protocol:
1. Read TASK.md, STATUS.md, and DECISIONS.md.
2. Inspect the current repository and tests.
3. Select the first unblocked TODO task whose dependencies are DONE, unless STATUS.md names a truthful IN_PROGRESS task.
4. Mark the selected task IN_PROGRESS in TASK.md and update STATUS.md before implementation.
5. Implement one small vertical slice at a time, including tests.
6. Run the relevant checks. Record exact commands and results.
7. Mark a task DONE only when its acceptance criteria are met and evidence is recorded.
8. Before stopping or when context is becoming limited, checkpoint STATUS.md with files changed, tests, blockers, and the exact next action. Do not leave half-written files.

Do not ask the user to repeat requirements already present in the repository. Make reasonable implementation choices, but record any architectural deviation as an ADR in DECISIONS.md before relying on it.

Hard constraints:
- No hard-coded symbols in the engine or strategy logic.
- One strategy per file under strategies/.
- No same-bar fill for a signal created from that bar close.
- Offline deterministic backtests by default.
- Explicit provider/feed, timezone, costs, fill policy, data manifest, and random seed for every run.
- No silent data repair, forward filling, provider mixing, or look-ahead.
- Secrets only through environment variables or an ignored .env file.
- Core tests must run without internet access or API keys.

Begin with the current task in STATUS.md and continue through TASK.md as far as can be completed safely in this session.